<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.park.loginscene">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginScene">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Home" />
        <activity android:name=".mypage" />
        <activity android:name=".member" />
        <activity android:name=".personaltraining" />
        <activity android:name=".information" />
        <activity android:name=".schedule" />
        <activity android:name=".attendance" />
        <activity android:name=".physical" />
        <activity android:name=".pay_page" />
        <activity android:name=".pay_finish" />
        <activity android:name=".PT" />
        <activity android:name=".GT" />
    </application>

</manifest>